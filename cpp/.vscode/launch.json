{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run cf4pwm (prompt URI)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/Debug/bin/Debug/cf4pwm_runner_win.exe",
      "args": [
        "--uri", "${input:radiouri}",
        "--rate", "500",
        "--csv", "metrics.csv",
        "--xyz", "${input:xyz}",
        "--takeoff-T", "${input:takeoff_T}",
        "--land-T", "${input:land_T}"
      ],
      "cwd": "${workspaceFolder}/build/Debug/bin/Debug",
      "console": "integratedTerminal",
      "preLaunchTask": "Copy libusb dll (Debug)"
    }
  ],
  "inputs": [
    {
      "id": "radiouri",
      "type": "promptString",
      "description": "Enter radio URI (e.g. radio://0/90/2M)",
      "default": "radio://0/90/2M"
    },
    {
      "id": "xyz",
      "type": "promptString",
      "description": "Enter target x,y,z in mm (CSV)",
      "default": "0,0,700"
    },
    {
      "id": "takeoff_T",
      "type": "promptString",
      "description": "Takeoff duration in seconds (S-curve)",
      "default": "3"
    },
    {
      "id": "land_T",
      "type": "promptString",
      "description": "Landing duration in seconds (S-curve)",
      "default": "4"
    }
  ]
}
